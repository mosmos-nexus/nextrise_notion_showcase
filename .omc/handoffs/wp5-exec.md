# Handoff: WP5 (S3) → WP6+7 (S4/S5)

- **Decided**: s3a~s3d live (16 slides). iframe manager refactored to factory `makeIframeManager({slideId,iframeId,src,preloadDelay})` — instances mgr_s2b (4.5s preload) + mgr_s3b (12s stagger); deck.iframe.reload/forceCapture dispatch by ACTIVE slide id (verified isolated: F on s3b leaves s2b untouched). s3c = dedicated MCP diagram, 7 sequential rays (MCP group 4 + 협업 스택 3 incl. Oopy), auto-play. s3d = metrics + takeaway + QR badge + mos-working (deck-assets).
- **Lead fixes applied post-worker** (worker hit token limit before verify, again): (1) registerHook now COMPOSES duplicate registrations (was Object.assign overwrite — s3b's pan hook was eating the iframe hook's onEnter; future s5b/s6 hooks get this safety too); (2) s3b capture layer asset swapped viewport-top → tall homepage.png (1910×1811) so capture-pan has travel (SRC constants already matched); (3) CSS override `#s3b .capture-pan-inner .capture-base{position:static;height:auto}` — global contain rule was killing overflow; + reduced-motion static block; (4) pan stops sort fixed ascending→DESCENDING (ascending consumed the deep stop as start point → zero motion). Verified: capture-mode pan travels with zoom holds; blocked fallback ✓; live iframe ✓ (walk had zero deck-origin errors).
- **Known third-party console noise** (embedded sites, NOT deck defects — verifier must filter): React #418/#423/#425 hydration, Highcharts accessibility warning, occasional transient 500 from hon2yt2ch (manager falls back to capture correctly; R recovers).
- **Polish backlog additions**: s3c kicker clipped by progress pill at 1280-wide windows (1920 fine — same chrome-scale class as s2c); s3c left half airy (pills could fan in an arc — cosmetic); s3a mid-gap (cosmetic).
- **For WP6+7**: 16 slides currently; s4a/s4b/s4c then s5→rename s5a + new s5b HOLD. registerHook composition is now safe for multi-behavior slides. Reset hooks in show() still reference 's5'/'s6' ids — WP6+7 MUST remap when renaming (C3). Mos srcs: s0/s2a still point at design-system originals — switch ALL to deck-assets/ in WP6 per task #6.

## Addendum after WP6+7 (lead verification)
- WP6+7 verified by lead (worker truncated pre-verify again): 17 slides; s5a zoom→s5b hold landing ✓; wheel/touch ignored on hold ✓; back-nav restores clean s5a ✓; #16 resume re-arms hold ✓; Mos all deck-assets ✓; s4b orbit regression ✓.
- s2c pan sort fix APPLIED (descending; end frame now translateY(-386) = bottom) — pending-fixes.md resolved & removed.
